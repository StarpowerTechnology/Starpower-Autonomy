Agent: Savvy
Query: FastMCP 2.0 from_fastapi example site:github.com OR site:fastapi.com
Date: 20260317_142226
──────────────────────────────────────────────────
[1] Title: Introducing FastMCP 2.0 - Mostly Harmless
[1] URL: https://jlowin.dev/blog/fastmcp-2
[1] Content: You can `from mcp.server.fastmcp import FastMCP` and be up and running in minutes. I introduced FastMCP with the tagline “because life’s too short for boilerplate,” focusing on making it easy to create MCP servers without getting bogged down in protocol details. FastMCP can now proxy any MCP server, turning it into a FastMCP server that’s compatible with all other features, including composition. # Point a client at *any* backend MCP server (local FastMCP instance, remote SSE, local script...). backend_client = Client("http://api.example.com/mcp/sse") # e.g., a remote SSE server. proxy_server = FastMCP.from_client(backend_client, name="MyProxy"). FastMCP 2.0 makes this trivial by automatically generating MCP servers from OpenAPI specs or FastAPI apps. FastMCP 2.0 introduces a completely **new client infrastructure** designed for robust interaction with any MCP server, supporting all major transports and even in-memory transport when working with local FastMCP servers. Imagine a server 
...[truncated]

[2] Title: FastAPI 🤝 FastMCP
[2] URL: https://gofastmcp.com/integrations/fastapi
[2] Content: # Create FastAPI app. app = FastAPI(title="E-commerce API", version="1.0.0"). @app.get("/products", response_model=list[ProductResponse]). @app.get("/products/{product_id}", response_model=ProductResponse). @app.post("/products", response_model=ProductResponse). @app.put("/products/{product_id}", response_model=ProductResponse). ## ​ Generating an MCP Server. mcp = FastMCP.from_fastapi(app=app). mcp = FastMCP.from_fastapi(app=app). """Get a product by ID.""". ### ​ Interacting with the MCP Server. mcp = FastMCP.from_fastapi(app=app). # Now: API at /products/*, MCP at /analytics/mcp/`. # - GET /products/{id} → ResourceTemplate. # Add authentication to your FastAPI app. # Create MCP server with authentication headers. ## ​ Mounting an MCP Server. # Create MCP server. # Create ASGI app from MCP server. mcp_app = mcp.http_app(path='/mcp'). app = FastAPI(title="E-commerce API", lifespan=mcp_app.lifespan). # Mount the MCP server. Generate MCP server from your API. mcp = FastMCP.from_fastapi(
...[truncated]

[3] Title: Adding an MCP Server to your FastAPI app using FastMCP
[3] URL: https://medium.com/mindfully-ai/adding-an-mcp-server-to-your-fastapi-app-using-fastmcp-e5239afe88d6
[3] Content: # Adding an MCP Server to your FastAPI app using FastMCP. 1. Convert all your APIs to MCP Resources and Tools (Generating MCP Server **FROM** FastAPI app). (Mount MCP server **INTO** FastAPI app). from fastmcp import FastMCPfrom fastapi import FastAPI# Create MCP server before creating your FastAPI appmcp = FastMCP("My MCP Server")@mcp.tooldef create_employee(empInfo: EmployeeInfo) -> EmployeeInfo:    """Point to same service layer application as of API"""    retrun serviceLayerCreateEmployee()    # Create ASGI app from MCP servermcp_app = mcp.http_app(path='/mcp')#how to create combined lifespan is shown after this exampleapp = FastAPI(title="Employee Mgmt System", lifespan=combined_lifespan)# Mount the MCP serverapp.mount("/employeeMcp", mcp_app)# Now: API at /employee/*, and MCP at /employeeMcp/mcp/ from from. # Create MCP server before creating your FastAPI app FastMCP "My MCP Server" create_employee empInfo "" "Point to same service layer application as of API" "" serviceLayerCrea
...[truncated]

[4] Title: Allow Custom Description Format via FastAPI Docstrings ...
[4] URL: https://github.com/PrefectHQ/fastmcp/pull/1756
[4] Content: Currently, FastMCP.from_fastapi() auto-generates verbose tool descriptions by combining FastAPI's description parameter with detailed Query

[5] Title: Turn Your FastAPI API into MCP Tools in Minutes
[5] URL: https://medium.com/@mail.fede.cesarini/turn-your-fastapi-api-into-mcp-tools-in-minutes-f9c2a92066d7
[5] Content: From REST to AI in one line. The transformation: FastMCP.from_fastapi(app=app). That's it. One line unlocks compatibility with every major AI
